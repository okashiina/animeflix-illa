{
  "name": "animeflix-source-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted anime source resolver: multi-provider scraper + HLS proxy + subtitles. Deploy on a VPS (see docs/VPS-SETUP.md). Frontend falls back to embed players when this returns mode:'embed'.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "aniwatch": "^2.27.9",
    "fastify": "^4.28.1",
    "lru-cache": "^10.4.3",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
